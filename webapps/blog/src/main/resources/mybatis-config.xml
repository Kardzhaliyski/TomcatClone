<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <setting name="logImpl" value="LOG4J2"/>
    </settings>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper class="com.github.kardzhaliyski.blog.dao.mapper.PostMapper"/>
        <mapper class="com.github.kardzhaliyski.blog.dao.mapper.CommentMapper"/>
        <mapper class="com.github.kardzhaliyski.blog.dao.mapper.UserMapper"/>
        <mapper class="com.github.kardzhaliyski.blog.dao.mapper.AuthTokenMapper"/>
    </mappers>
</configuration>
